<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Error - Elite Tailoring</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  <body>
    <div class="container mx-auto px-4 py-12">
      <h1 class="text-2xl font-bold mb-4"><%= message %></h1>

      <% if (typeof error !== 'undefined') { %> <% if (error.status) { %>
      <h2 class="text-xl mb-2">Status: <%= error.status %></h2>
      <% } %> <% if (error.stack) { %>
      <div class="bg-gray-100 p-4 rounded">
        <pre class="text-sm text-gray-800"><%= error.stack %></pre>
      </div>
      <% } %> <% } %>

      <a
        href="/"
        class="mt-6 inline-block px-4 py-2 bg-gray-800 text-white rounded hover:bg-gray-700"
      >
        Return to Home
      </a>
    </div>
  </body>
</html>
